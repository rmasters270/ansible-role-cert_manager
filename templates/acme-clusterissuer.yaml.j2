apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ _cluster_issuer_name }}
spec:
  acme:
    email: {{ letsencrypt_email }}
    server: {{ _cert_issuer_server }}

    privateKeySecretRef:
      name: issuer-account-key
    solvers:
      - dns01:
          cloudflare:
            apiTokenSecretRef:
              name: cloudflare-api-key-secret
              key: email
            apiTokenSecretRef:
              name: cloudflare-api-key-secret
              key: api-token
